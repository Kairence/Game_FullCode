# Player Commands

## 1. 개요
일반 유저들이 경제 수치 확인, 파티 어그로 테이블 모니터링, 가문 상태 등을 인게임에서 호출하기 위한 커스텀 명령어(`[명령어`) 모음입니다.

## 2. 의존성 (Dependencies)
*   내부 로직은 `TownSocietyGump`나 `EconomyCitizenMainGump` 등 각종 UI(Gump) 시스템을 호출하는 브릿지 역할을 합니다.
